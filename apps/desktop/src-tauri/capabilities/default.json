{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "WP0 shell windows",
  "windows": ["settings", "permissions", "calibration"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
